{
  "name": "osmosis-case-study",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:browser": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "dependencies": {
    "@preact/signals": "^1.3.1",
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@preact/preset-vite": "^2.10.2",
    "@types/node": "^24.10.1",
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vitest": "^3.1.1"
  }
}
